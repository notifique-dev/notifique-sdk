/*
 * Notifique API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package dev.notifique.sdk.openapi.models;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import dev.notifique.sdk.openapi.models.NtfAutoTriggerConfigChannelInbound;
import dev.notifique.sdk.openapi.models.NtfAutoTriggerConfigEvent;
import dev.notifique.sdk.openapi.models.NtfAutoTriggerConfigWebhook;
import dev.notifique.sdk.openapi.models.NtfAutoTriggerConfigWebhookPayloadMappingsInner;
import dev.notifique.sdk.openapi.models.NtfAutoTriggerConfigWhatsappCall;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import dev.notifique.sdk.ApiClient;
import dev.notifique.sdk.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.25.0-SNAPSHOT")
@JsonDeserialize(using = NtfAutoStepTriggerConfig.NtfAutoStepTriggerConfigDeserializer.class)
@JsonSerialize(using = NtfAutoStepTriggerConfig.NtfAutoStepTriggerConfigSerializer.class)
public class NtfAutoStepTriggerConfig extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(NtfAutoStepTriggerConfig.class.getName());

    public static class NtfAutoStepTriggerConfigSerializer extends StdSerializer<NtfAutoStepTriggerConfig> {
        public NtfAutoStepTriggerConfigSerializer(Class<NtfAutoStepTriggerConfig> t) {
            super(t);
        }

        public NtfAutoStepTriggerConfigSerializer() {
            this(null);
        }

        @Override
        public void serialize(NtfAutoStepTriggerConfig value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class NtfAutoStepTriggerConfigDeserializer extends StdDeserializer<NtfAutoStepTriggerConfig> {
        public NtfAutoStepTriggerConfigDeserializer() {
            this(NtfAutoStepTriggerConfig.class);
        }

        public NtfAutoStepTriggerConfigDeserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public NtfAutoStepTriggerConfig deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = ctxt.readTree(jp);
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // deserialize NtfAutoTriggerConfigChannelInbound
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (NtfAutoTriggerConfigChannelInbound.class.equals(Integer.class) || NtfAutoTriggerConfigChannelInbound.class.equals(Long.class) || NtfAutoTriggerConfigChannelInbound.class.equals(Float.class) || NtfAutoTriggerConfigChannelInbound.class.equals(Double.class) || NtfAutoTriggerConfigChannelInbound.class.equals(Boolean.class) || NtfAutoTriggerConfigChannelInbound.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((NtfAutoTriggerConfigChannelInbound.class.equals(Integer.class) || NtfAutoTriggerConfigChannelInbound.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((NtfAutoTriggerConfigChannelInbound.class.equals(Float.class) || NtfAutoTriggerConfigChannelInbound.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (NtfAutoTriggerConfigChannelInbound.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (NtfAutoTriggerConfigChannelInbound.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(NtfAutoTriggerConfigChannelInbound.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'NtfAutoTriggerConfigChannelInbound'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'NtfAutoTriggerConfigChannelInbound'", e);
            }

            // deserialize NtfAutoTriggerConfigEvent
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (NtfAutoTriggerConfigEvent.class.equals(Integer.class) || NtfAutoTriggerConfigEvent.class.equals(Long.class) || NtfAutoTriggerConfigEvent.class.equals(Float.class) || NtfAutoTriggerConfigEvent.class.equals(Double.class) || NtfAutoTriggerConfigEvent.class.equals(Boolean.class) || NtfAutoTriggerConfigEvent.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((NtfAutoTriggerConfigEvent.class.equals(Integer.class) || NtfAutoTriggerConfigEvent.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((NtfAutoTriggerConfigEvent.class.equals(Float.class) || NtfAutoTriggerConfigEvent.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (NtfAutoTriggerConfigEvent.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (NtfAutoTriggerConfigEvent.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(NtfAutoTriggerConfigEvent.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'NtfAutoTriggerConfigEvent'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'NtfAutoTriggerConfigEvent'", e);
            }

            // deserialize NtfAutoTriggerConfigWebhook
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (NtfAutoTriggerConfigWebhook.class.equals(Integer.class) || NtfAutoTriggerConfigWebhook.class.equals(Long.class) || NtfAutoTriggerConfigWebhook.class.equals(Float.class) || NtfAutoTriggerConfigWebhook.class.equals(Double.class) || NtfAutoTriggerConfigWebhook.class.equals(Boolean.class) || NtfAutoTriggerConfigWebhook.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((NtfAutoTriggerConfigWebhook.class.equals(Integer.class) || NtfAutoTriggerConfigWebhook.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((NtfAutoTriggerConfigWebhook.class.equals(Float.class) || NtfAutoTriggerConfigWebhook.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (NtfAutoTriggerConfigWebhook.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (NtfAutoTriggerConfigWebhook.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(NtfAutoTriggerConfigWebhook.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'NtfAutoTriggerConfigWebhook'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'NtfAutoTriggerConfigWebhook'", e);
            }

            // deserialize NtfAutoTriggerConfigWhatsappCall
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (NtfAutoTriggerConfigWhatsappCall.class.equals(Integer.class) || NtfAutoTriggerConfigWhatsappCall.class.equals(Long.class) || NtfAutoTriggerConfigWhatsappCall.class.equals(Float.class) || NtfAutoTriggerConfigWhatsappCall.class.equals(Double.class) || NtfAutoTriggerConfigWhatsappCall.class.equals(Boolean.class) || NtfAutoTriggerConfigWhatsappCall.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((NtfAutoTriggerConfigWhatsappCall.class.equals(Integer.class) || NtfAutoTriggerConfigWhatsappCall.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((NtfAutoTriggerConfigWhatsappCall.class.equals(Float.class) || NtfAutoTriggerConfigWhatsappCall.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (NtfAutoTriggerConfigWhatsappCall.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (NtfAutoTriggerConfigWhatsappCall.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(NtfAutoTriggerConfigWhatsappCall.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'NtfAutoTriggerConfigWhatsappCall'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'NtfAutoTriggerConfigWhatsappCall'", e);
            }

            if (match == 1) {
                NtfAutoStepTriggerConfig ret = new NtfAutoStepTriggerConfig();
                ret.setActualInstance(deserialized);
                return ret;
            }
            throw new IOException(String.format(java.util.Locale.ROOT, "Failed deserialization for NtfAutoStepTriggerConfig: %d classes match result, expected 1", match));
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public NtfAutoStepTriggerConfig getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "NtfAutoStepTriggerConfig cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public NtfAutoStepTriggerConfig() {
        super("oneOf", Boolean.FALSE);
    }

    public NtfAutoStepTriggerConfig(NtfAutoTriggerConfigChannelInbound o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public NtfAutoStepTriggerConfig(NtfAutoTriggerConfigEvent o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public NtfAutoStepTriggerConfig(NtfAutoTriggerConfigWebhook o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public NtfAutoStepTriggerConfig(NtfAutoTriggerConfigWhatsappCall o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("NtfAutoTriggerConfigChannelInbound", NtfAutoTriggerConfigChannelInbound.class);
        schemas.put("NtfAutoTriggerConfigEvent", NtfAutoTriggerConfigEvent.class);
        schemas.put("NtfAutoTriggerConfigWebhook", NtfAutoTriggerConfigWebhook.class);
        schemas.put("NtfAutoTriggerConfigWhatsappCall", NtfAutoTriggerConfigWhatsappCall.class);
        JSON.registerDescendants(NtfAutoStepTriggerConfig.class, Collections.unmodifiableMap(schemas));
        // Initialize and register the discriminator mappings.
        Map<String, Class<?>> mappings = new HashMap<String, Class<?>>();
        mappings.put("channelInbound", NtfAutoTriggerConfigChannelInbound.class);
        mappings.put("event", NtfAutoTriggerConfigEvent.class);
        mappings.put("webhook", NtfAutoTriggerConfigWebhook.class);
        mappings.put("whatsappCall", NtfAutoTriggerConfigWhatsappCall.class);
        mappings.put("NtfAuto_StepTrigger_config", NtfAutoStepTriggerConfig.class);
        JSON.registerDiscriminator(NtfAutoStepTriggerConfig.class, "triggerKind", mappings);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return NtfAutoStepTriggerConfig.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * NtfAutoTriggerConfigChannelInbound, NtfAutoTriggerConfigEvent, NtfAutoTriggerConfigWebhook, NtfAutoTriggerConfigWhatsappCall
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(NtfAutoTriggerConfigChannelInbound.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(NtfAutoTriggerConfigEvent.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(NtfAutoTriggerConfigWebhook.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(NtfAutoTriggerConfigWhatsappCall.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be NtfAutoTriggerConfigChannelInbound, NtfAutoTriggerConfigEvent, NtfAutoTriggerConfigWebhook, NtfAutoTriggerConfigWhatsappCall");
    }

    /**
     * Get the actual instance, which can be the following:
     * NtfAutoTriggerConfigChannelInbound, NtfAutoTriggerConfigEvent, NtfAutoTriggerConfigWebhook, NtfAutoTriggerConfigWhatsappCall
     *
     * @return The actual instance (NtfAutoTriggerConfigChannelInbound, NtfAutoTriggerConfigEvent, NtfAutoTriggerConfigWebhook, NtfAutoTriggerConfigWhatsappCall)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `NtfAutoTriggerConfigChannelInbound`. If the actual instance is not `NtfAutoTriggerConfigChannelInbound`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `NtfAutoTriggerConfigChannelInbound`
     * @throws ClassCastException if the instance is not `NtfAutoTriggerConfigChannelInbound`
     */
    public NtfAutoTriggerConfigChannelInbound getNtfAutoTriggerConfigChannelInbound() throws ClassCastException {
        return (NtfAutoTriggerConfigChannelInbound)super.getActualInstance();
    }

    /**
     * Get the actual instance of `NtfAutoTriggerConfigEvent`. If the actual instance is not `NtfAutoTriggerConfigEvent`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `NtfAutoTriggerConfigEvent`
     * @throws ClassCastException if the instance is not `NtfAutoTriggerConfigEvent`
     */
    public NtfAutoTriggerConfigEvent getNtfAutoTriggerConfigEvent() throws ClassCastException {
        return (NtfAutoTriggerConfigEvent)super.getActualInstance();
    }

    /**
     * Get the actual instance of `NtfAutoTriggerConfigWebhook`. If the actual instance is not `NtfAutoTriggerConfigWebhook`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `NtfAutoTriggerConfigWebhook`
     * @throws ClassCastException if the instance is not `NtfAutoTriggerConfigWebhook`
     */
    public NtfAutoTriggerConfigWebhook getNtfAutoTriggerConfigWebhook() throws ClassCastException {
        return (NtfAutoTriggerConfigWebhook)super.getActualInstance();
    }

    /**
     * Get the actual instance of `NtfAutoTriggerConfigWhatsappCall`. If the actual instance is not `NtfAutoTriggerConfigWhatsappCall`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `NtfAutoTriggerConfigWhatsappCall`
     * @throws ClassCastException if the instance is not `NtfAutoTriggerConfigWhatsappCall`
     */
    public NtfAutoTriggerConfigWhatsappCall getNtfAutoTriggerConfigWhatsappCall() throws ClassCastException {
        return (NtfAutoTriggerConfigWhatsappCall)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof NtfAutoTriggerConfigEvent) {
        if (getActualInstance() != null) {
          joiner.add(((NtfAutoTriggerConfigEvent)getActualInstance()).toUrlQueryString(prefix + "one_of_0" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof NtfAutoTriggerConfigChannelInbound) {
        if (getActualInstance() != null) {
          joiner.add(((NtfAutoTriggerConfigChannelInbound)getActualInstance()).toUrlQueryString(prefix + "one_of_1" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof NtfAutoTriggerConfigWebhook) {
        if (getActualInstance() != null) {
          joiner.add(((NtfAutoTriggerConfigWebhook)getActualInstance()).toUrlQueryString(prefix + "one_of_2" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof NtfAutoTriggerConfigWhatsappCall) {
        if (getActualInstance() != null) {
          joiner.add(((NtfAutoTriggerConfigWhatsappCall)getActualInstance()).toUrlQueryString(prefix + "one_of_3" + suffix));
        }
        return joiner.toString();
    }
    return null;
  }

}

